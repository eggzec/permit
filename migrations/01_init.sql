-- Sample database initialization script
-- This file runs when the PostgreSQL container initializes for the first time
-- Place your schema creation, data seeding, or migration scripts here

-- Example: Create a simple table
-- CREATE TABLE IF NOT EXISTS example (
--     id SERIAL PRIMARY KEY,
--     name VARCHAR(255) NOT NULL,
--     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
-- );

-- Add your SQL statements below
-- Note: Scripts run in alphabetical order, so prefix with numbers if order matters (e.g., 01_init.sql, 02_seed.sql)